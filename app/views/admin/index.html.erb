<h1>Users</h1>
<table>
  <thead>
    <thead>
      <tr>
        <th>Email</th><th>Role</th>
      </tr>
    </thead>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= user.roles.last.name %></td>
      <td>
        <%= form_tag("/admin/" + user.id.to_s + "?role=owner", method: :PUT) do %>
          <input type="submit" value="Make Owner"/>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
